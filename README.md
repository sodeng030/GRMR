1. 백엔드 실행하는 방법

## nodemon index.js
(저장할때마다 서버 다시 열림)

## ngrok http 3000

2. DB 연결시 주의점

host: '이부분'
https:// << 이거 빼고 적기

3. git commit 하기

git add .
git commit -m "UI 디자인 수정 및 버튼 추가"
git push origin 내브랜치이름